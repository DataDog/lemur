# Run `make up-reqs` to update pinned dependencies in requirement text files

flake8
Flake8-pyproject
pre-commit
invoke
twine
nodeenv
-r requirements-tests.txt

# === Datadog-specific additions ===
cryptography==46.0.7  # Must keep pinned for FIPS support (do not sync from upstream)
